# `@career-compiler/core`

Career Compiler 的 framework-independent domain model：`CareerEvidence`、`CareerFact`、`CareerProfile`、`CareerIR`、source/renderer/repository contracts，以及可验证的 candidate fact pipeline。

该 package 不依赖 SQLite、HTTP client 或任何 AI provider。
